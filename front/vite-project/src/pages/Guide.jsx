// src/pages/Guide.jsx
/**
 * صفحه‌ی «راهنما»
 * --------------------------------------------------------------------
 * این صفحه فقط اسکلت/چارچوب نمایشی را فراهم می‌کند و منطق اصلی
 * واکشی و نمایش دسته‌ها و آیتم‌ها را به کامپوننت <GuideBox/> می‌سپارد.
 *
 * چه کار می‌کند؟
 *   - یک ظرف (container) راست‌به‌چپ با فاصله‌های استاندارد ایجاد می‌کند.
 *   - تیتر و توضیح کوتاه در بالای صفحه نشان می‌دهد.
 *   - سپس <GuideBox/> را رندر می‌کند تا دسته‌های راهنما و آیتم‌ها
 *     (قابل/غیرقابل/آماده‌سازی/نکات) را با آکاردئون و جستجو نمایش دهد.
 *
 * نکات:
 *   - جهت صفحه با dir="rtl" تنظیم شده است تا تایپوگرافی فارسی درست باشد.
 *   - کلاس‌های Tailwind (container, mx-auto, px-4, py-8, …) فقط برای استایل هستند.
 *   - اگر لازم بود سئوی صفحه را بهتر کنید، می‌توانید در این فایل از کتابخانه‌هایی
 *     مثل react-helmet برای تنظیم <title> و متاها استفاده کنید.
 *   - <GuideBox/> خودش مسئول فراخوانی API، جستجو، و باز/بسته‌شدن بخش‌هاست
 *     و با هَش آدرس (/#slug) نیز هماهنگ می‌شود (در همان کامپوننت پیاده‌سازی شده).
 */

import GuideBox from "../components/GuideBox";

export default function Guide() {
  return (
    // ظرف اصلی صفحه با راست‌چین‌سازی
    <div dir="rtl" className="container mx-auto px-4 py-8 text-right">
      {/* تیتر صفحه */}
      <h1 className="text-2xl font-bold mb-2">راهنمای کامل تفکیک زباله</h1>

      {/* توضیح کوتاه زیر تیتر */}
      <p className="text-slate-600 mb-6">
        دسته‌ها، مثال‌های قابل/غیرقابل بازیافت و روش آماده‌سازی را اینجا پیدا کنید.
      </p>

      {/* بدنه‌ی پویا: لیست دسته‌ها و آیتم‌ها (واگذاری به GuideBox) */}
      <GuideBox />
    </div>
  );
}
