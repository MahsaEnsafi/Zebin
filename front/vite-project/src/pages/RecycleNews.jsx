// src/pages/RecycleNews.jsx
/**
 * صفحه «اخبار بازیافت»
 * -----------------------------------------------------------------------------
 * نقش این صفحه نمایش تیتر، توضیح کوتاه، و سپس لیست خبرهای حوزه بازیافت است.
 * خودِ لیست خبرها توسط کامپوننت <NewsBox /> واکشی/رندر می‌شود.
 *
 * نکات فنی:
 * - جهت صفحه RTL است تا متن فارسی درست نمایش داده شود.
 * - از یک container با فاصله‌های استاندارد Tailwind استفاده شده است.
 * - کلاس تایپوگرافی "text-right" برای راست‌چین‌بودن متن استفاده می‌شود.
 * - اگر نیاز به سئو بیشتر دارید، می‌توانید از React Helmet برای افزودن <title> و meta استفاده کنید.
 *
 * تغییر کوچک:
 * - اشتباه تایپی کلاس Tailwind از "text-righ" به "text-right" اصلاح شد.
 */

import React from "react";
import NewsBox from "../components/NewsBox"; // لیست/کارت‌های خبر را مدیریت می‌کند

export default function RecycleNews() {
  return (
    // container اصلی صفحه: راست‌به‌چپ، حاشیه‌های افقی و عمودی استاندارد
    <main dir="rtl" className="container mx-auto px-4 py-8 text-right">
      {/* تیتر صفحه */}
      <h1 className="text-2xl font-bold mb-2">اخبار بازیافت</h1>

      {/* توضیح کوتاه بالای لیست خبرها */}
      <p className="text-slate-600 mb-6">
        در این بخش می‌توانید آخرین خبرها دربارهٔ تحولات، فناوری‌ها و پروژه‌های مرتبط
        با بازیافت در ایران و جهان را مطالعه کنید.
      </p>

      {/* لیست خبرها: واکشی و مدیریت وضعیت بارگذاری/خطا داخل NewsBox انجام می‌شود */}
      <NewsBox />
    </main>
  );
}
